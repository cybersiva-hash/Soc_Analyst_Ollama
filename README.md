# Soc_Analyst_Ollama
AI-powered SOC alert triage pipeline that captures network traffic, detects anomalous volume spikes via tshark, and uses a local LLM (Qwen2.5 via Ollama) to classify alerts as benign/suspicious/malicious with structured JSON verdicts — fully offline, no external API calls.
